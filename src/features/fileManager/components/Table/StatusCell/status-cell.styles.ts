import styled from "styled-components";

export const Syncing = styled.div`
  color: ${(props) => props.theme.colors.gray2};
`;

export const SyncingInProcess = styled.div`
  color: ${(props) => props.theme.colors.black};
`;
