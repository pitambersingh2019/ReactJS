import { SpinnerContainer, SpinnerWrapper } from "./spinner.styles";

const Spinner = () => {
  return (
    <SpinnerWrapper>
      <SpinnerContainer />
    </SpinnerWrapper>
  );
};

export default Spinner;
